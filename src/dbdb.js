import React from "react";
import axios from "axios";

const dbdb = () => {
  return (
    <div>
      <button onClick={() => onDataHandler()}></button>
    </div>
  );
};

function onDataHandler(){
    const axios
}